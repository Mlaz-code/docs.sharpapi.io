# Quick Start

Get started with SharpAPI in 2 minutes.

## Get Your API Key

1. **Sign Up** - Create a free account at [sharpapi.io/signup](https://sharpapi.io/signup)
2. **Get Your Key** - Copy your API key from the dashboard. It starts with `sk_live_` or `sk_test_`
3. **Make Your First Call** - Use the examples below to fetch live odds

## Your First API Call

### cURL

```bash
curl -X GET "https://sharpapi.io/api/v1/odds" \
  -H "X-API-Key: YOUR_API_KEY"
```

### JavaScript

```javascript
const response = await fetch('https://sharpapi.io/api/v1/odds', {
  headers: {
    'X-API-Key': 'YOUR_API_KEY'
  }
});

const data = await response.json();
console.log(`Found ${data.count} odds from ${data.books.length} books`);
```

### Python

```python
import requests

response = requests.get(
    'https://sharpapi.io/api/v1/odds',
    headers={'X-API-Key': 'YOUR_API_KEY'}
)

data = response.json()
print(f"Found {data['count']} odds from {len(data['books'])} books")
```

### TypeScript

```typescript
import { SharpApiClient } from '@sharpapi/client';

const client = new SharpApiClient('YOUR_API_KEY');
const odds = await client.getOdds();

console.log(`Found ${odds.count} odds`);
```

## Example Response

```json
{
  "success": true,
  "timestamp": "2024-01-21T02:10:37.846Z",
  "count": 3095,
  "books": ["draftkings", "pinnacle"],
  "data": {
    "draftkings": [
      {
        "id": "draftkings_33483153_moneyline_PHO",
        "sportsbook": "draftkings",
        "sport": "nba",
        "homeTeam": "PHI 76ers",
        "awayTeam": "PHO Suns",
        "marketType": "moneyline",
        "selection": "PHO Suns",
        "odds": {
          "american": -150,
          "decimal": 1.67,
          "probability": 0.60
        },
        "isLive": false
      }
    ]
  }
}
```

## Next Steps

- [Get +EV Bets](/api-reference/ev) - Find positive expected value opportunities
- [Find Arbitrage](/api-reference/arbitrage) - Detect risk-free profit opportunities
- [Stream Live](/streaming/overview) - Real-time updates via SSE
- [Rate Limits](/authentication#rate-limits) - Understand usage limits
