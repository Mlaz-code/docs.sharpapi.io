# Leagues

Get a list of available leagues with optional sport filtering.

```
GET /api/v1/leagues
```

## Authentication

Requires API key. Available to all tiers.

## Query Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `sport` | string | No | Filter by sport (e.g., `basketball`) |

## Response

| Field | Type | Description |
|-------|------|-------------|
| `success` | boolean | Whether the request succeeded |
| `timestamp` | string | ISO 8601 timestamp |
| `filter` | object | Applied filters |
| `leagues` | array | List of leagues |

### League Object

| Field | Type | Description |
|-------|------|-------------|
| `id` | string | League identifier (lowercase) |
| `name` | string | Display name |
| `sport` | string | Parent sport |
| `eventCount` | number | Events with odds |
| `liveEventCount` | number | Currently live events |

### Example Request

```bash cURL
curl -X GET "https://sharpapi.io/api/v1/leagues?sport=basketball" \
  -H "X-API-Key: YOUR_API_KEY"
```

```javascript JavaScript
const response = await fetch(
  'https://sharpapi.io/api/v1/leagues?sport=basketball',
  { headers: { 'X-API-Key': 'YOUR_API_KEY' } }
);
const data = await response.json();
```

```python Python
import requests

response = requests.get(
    'https://sharpapi.io/api/v1/leagues',
    params={'sport': 'basketball'},
    headers={'X-API-Key': 'YOUR_API_KEY'}
)
data = response.json()
```

### Response

```json 200
{
  "success": true,
  "timestamp": "2024-01-15T10:30:00.000Z",
  "filter": {
    "sport": "basketball"
  },
  "leagues": [
    {
      "id": "nba",
      "name": "NBA",
      "sport": "basketball",
      "eventCount": 15,
      "liveEventCount": 2
    },
    {
      "id": "ncaab",
      "name": "NCAAB",
      "sport": "basketball",
      "eventCount": 27,
      "liveEventCount": 1
    },
    {
      "id": "wnba",
      "name": "WNBA",
      "sport": "basketball",
      "eventCount": 0,
      "liveEventCount": 0
    }
  ]
}
```

```json 401
{
  "error": "Invalid API key",
  "code": "invalid_api_key"
}
```

## Popular Leagues by Sport

### Basketball
- NBA - National Basketball Association
- NCAAB - NCAA Men's Basketball
- WNBA - Women's National Basketball Association

### Football
- NFL - National Football League
- NCAAF - NCAA Football

### Hockey
- NHL - National Hockey League

### Baseball
- MLB - Major League Baseball

### Soccer
- EPL - English Premier League
- La Liga - Spanish La Liga
- Serie A - Italian Serie A
- Bundesliga - German Bundesliga
